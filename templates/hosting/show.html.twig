{% extends 'base.html.twig' %}

{% block title %}Hosting{% endblock %}

{% block body %}
    <h1>Hosting</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hosting.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hosting.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ hosting.address }}</td>
            </tr>
            <tr>
                <th>Price_per_night</th>
                <td>{{ hosting.pricePerNight }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ hosting.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hosting_index') }}">back to list</a>

    <a href="{{ path('hosting_edit', {'id': hosting.id}) }}">edit</a>

    {{ include('hosting/_delete_form.html.twig') }}
{% endblock %}
